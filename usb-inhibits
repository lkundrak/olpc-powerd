# permitted syntax:
#   id=vendor:id  (where vendor and id are each 4 digit hex, without "0x".
#   class=classcode  (where classcode is 2-digit hex, or one of:
#	"audio", "commc", "hid", "pid", "still", "print", "stor.", "hub",
#	"data", "scard", "c-sec", "video", "perhc", "diagd", "wlcon", "misc",
#	"app.", "vend.")   (class may be either device or interface class)
# 
# no suspend when using input devices, since they can't wake us up.
class=hid
